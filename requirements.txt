# Core data processing
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
openpyxl>=3.1

# Optional timeseries models (auto-detected; script falls back if missing)
statsmodels>=0.14  # SARIMAX modeling
prophet>=1.1.5     # Facebook Prophet (may take time to install)
xgboost>=2.0       # XGBoost for share/ARTES models

# Install all:
# pip install -r requirements.txt
#
# Or minimal (no optional models):
# pip install pandas>=2.0 numpy>=1.24 matplotlib>=3.7 scikit-learn>=1.3 openpyxl>=3.1
